<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Web" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Web" %>">
    <%= stylesheet_link_tag 'app', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%# Modernizr is required for Zurb Foundation %>
    <%= javascript_include_tag 'vendor/modernizr' %>

    <%= javascript_include_tag 'proton-1.0.0.min' %>
    <%= javascript_include_tag 'app', 'data-turbolinks-track' => 'reload' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <main role="main">
       <%= yield %>
    </main>
  </body>
</html>
